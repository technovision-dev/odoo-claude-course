# Security

## Groups and access

| Model | {Group A} | {Group B} |
|---|---|---|
| `{model}` | {read} | {read, write, create, delete} |

## Record rules

- `{model}`: {domain and why}.

## Data that leaves the server

{Emails sent and to whom; external APIs called and what is sent. "None" if none.}

## Elevated rights in code

{Each sudo() and why it is safe. "None" if none.}
