# Installation

## Requirements

- Odoo {18.0}, Community or Enterprise.
- Modules installed automatically as dependencies: {list}.
- Python packages: {none / list from external_dependencies}.

## Steps

1. Copy the `{module}` folder into a directory on your `addons_path`.
2. Restart Odoo.
3. Apps > Update Apps List, search "{Module Name}", Install.

## Check that it works

- {A menu that must now exist.}
- {A scheduled action that must now exist, and its interval.}

## Uninstalling

{What is removed (models, data) and what stays (e.g. chatter notes already posted).}
