<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_quotation_followup" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.quotation.followup</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button name="action_view_followups" type="object" class="oe_stat_button"
                        icon="fa-envelope-o" invisible="not followup_count">
                    <field name="followup_count" widget="statinfo" string="Follow-ups"/>
                </button>
            </div>
            <field name="validity_date" position="after">
                <field name="quotation_sent_date" invisible="not quotation_sent_date"/>
            </field>
        </field>
    </record>
</odoo>
