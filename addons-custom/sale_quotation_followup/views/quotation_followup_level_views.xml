<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="quotation_followup_level_view_list" model="ir.ui.view">
        <field name="name">quotation.followup.level.list</field>
        <field name="model">quotation.followup.level</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="days_after_sent"/>
                <field name="mail_template_id"/>
                <field name="create_activity"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>

    <record id="quotation_followup_level_view_form" model="ir.ui.view">
        <field name="name">quotation.followup.level.form</field>
        <field name="model">quotation.followup.level</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="days_after_sent"/>
                            <field name="active" invisible="1"/>
                        </group>
                        <group>
                            <field name="mail_template_id"/>
                            <field name="create_activity"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="quotation_followup_level_view_search" model="ir.ui.view">
        <field name="name">quotation.followup.level.search</field>
        <field name="model">quotation.followup.level</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <filter name="archived" string="Archived" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="quotation_followup_level_action" model="ir.actions.act_window">
        <field name="name">Follow-up Levels</field>
        <field name="res_model">quotation.followup.level</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first follow-up level</p>
            <p>For example: a friendly check-in 3 days after sending, and a last reminder after 14 days.</p>
        </field>
    </record>
</odoo>
